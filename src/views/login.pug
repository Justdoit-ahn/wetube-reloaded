extends base

block content
  if errorMessage 
    span=errorMessage
  form(method="POST")
    input(name="username",placeholder="Username", type="text", required)
    input(name="password",placeholder="Password", type="password", required)
    input(type="submit", value="로그인")
  hr
  div
    span 계정이 없나요?? 계정을 만드세요!   
    a(href='/join') 회원가입&rarr;